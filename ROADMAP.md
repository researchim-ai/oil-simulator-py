# ROADMAP - Симулятор нефтяного месторождения (oil-simulator-py)

## 0. Текущий статус

Проект реализует двумерное и трёхмерное гидродинамическое моделирование двухфазной (нефть-вода) системы
с двумя численными схемами (IMPES и полностью неявная), поддержкой GPU (PyTorch), базовой моделью скважин
(Писмен) и экспортом результатов в PNG/GIF/VTK.  Покрыт юнит-тестами и имеет примеры конфигурационных файлов.

## 0.1 Недавние улучшения

- Поддержка переменной пористости `φ(P)` с учётом сжимаемости породы.
- Корректный учёт сжимаемости флюидов в невязке и Якобиане (ρ(P)).
- Единая быстрая невязка `_compute_residual_full` для line-search и JFNK.
- Масштабирование аккумуляции/потоков по `dt`; сохранена размерность невязки в масса.
- Trust-region и clamping для `ΔP`, `ΔSw` (устойчивая линия поиска).
- Адаптивное уменьшение `dt` + fallback JFNK → IMPES, чтобы симуляция не падала в CI.
- Автоматический выбор линейного решателя: PETSc+BoomerAMG при наличии `petsc4py`; иначе `torch_gmres` (CPU/GPU).
- Trust-region по давлению: единицы ΔP переведены в МПа, радиус теперь инициализируется динамически.
- Реализован CPR-предобуславливатель: давление-блок → BoomerAMG, насыщенности → ω-Jacobi (ω = 0.8).
- Смягчён лимит `max_pressure_change` до 30 % от среднего давления, что устранило ранние разрывы шага.
- Лог авто-детектора PETSc теперь выводит точную причину фоллбэка (версии Python/библиотек).

## 0.2 План «Ньютон без срывов» (Q3-2025)

1. Надёжность на малых и больших сетках  
   • Векторизовать оставшиеся циклы при сборке Якобиана для O(N).  
   • Перейти на разреженное хранение `Jacobian` (torch.sparse_coo) + Krylov.
2. Предобуславливание и линейные решатели  
   • Jacobi/ILU0 на CPU (SciPy + pyamg).  
   • CUDA: использовать `torch.sparse_csr` + cuSPARSE-CG; fallback на CPU при OOM.  
   • Исследовать AMG (pyamg/hypre) как опцию.
3. Динамика шага по времени  
   • Error-controller: увеличивать `dt`, если относительная невязка < 0.1 после 2 итераций.  
   • Минимальная ширина шага `dt_min`, остановка симуляции при превышении лимита попыток.
4. Trust-region 2-нормы  
   • Автоматическое ограничение `‖Δx‖` вместо компонентного clamp.  
   • Подстройка радиуса на основе ratio actual/predicted reduction.
5. Совместимость CPU/GPU  
   • Вынос операций SciPy в адаптер; при `device==cuda` использовать cupy/cusolver.  
   • Тестовый фарм: `pytest -n auto` с параметризацией `cpu|cuda`.
6. Логирование и дебаг  
   • Лог-файлы per-step (JSON) с невязкой, итерациями, временем, успехами line-search.  
   • Флаг `SIM_DEBUG=1` включает сохранение промежуточных Jacobs/δ.
7. Тестовый набор  
   • 2D (50×50), 3D (40×40×10), SPE10 (отрывок) — smoke + regression на GPU/CPU.  
   • Сценарии с высоким `pc_scale`, многофазные стволы, высокий контраст μ.
8. Расширенный пакет устойчивости (Solver Reliability Pack)  
   • 🔹 Переменное масштабирование переменных (diag-scaling) для выравнивания диагоналей Якобиана.  
   • 🔹 CPR-FLEX/ABF разукрупнение: давление → AMG (BoomerAMG), насыщенности → ILU0/Jacobi.  
   • 🔹 Anderson / NGMRES ускорение поверх классического Ньютона и JFNK.  
   • 🔹 Pseudo-Transient Continuation с автоматическим α(t) → 0 при устойчивой сходимости.  
   • 🔹 CNV-контроллер шага времени: dt↑ если CNV<0.01 и итераций <5, dt↓ при CNV>0.2 или итераций>12.  
   • 🔹 Cubic / Wolfe line-search вместо геометрического back-tracking.  
   • 🔹 Escape-стратегии: Local Time Stepping + откат ∆t/3, если ‖F‖ растёт >1.5× три итерации.  
   • 🔹 Двусторонний trust-region (уже реализован) + статистика ρ для авто-настройки радиуса.  
   • 🔹 Поддержка матричного масштабирования/обратных диагоналей при сборке CPR.  

> Эти задачи формируют «уровень коммерческих симуляторов» над текущим базисом.

## 0.3 Ближайшие две недели (июнь 2025)

1. **Доделать CPR-предобуславливатель**  
   • Правильно формировать CSR-матрицу N×N только для давления; исправить ошибку `size(I)` в BoomerAMG.  
2. **Выбор устройства**  
   • Добавить параметр `device: cpu|cuda:0|cuda:1|ddp` в конфиг; обеспечить корректный перенос данных.  
3. **Мини-distributed запуск на 2 GPU**  
   • Простое разбиение по Z-слоям, обмен ghost-ячеек через `torch.distributed`.  
4. **Профилирование и baseline**  
   • Интегрировать `torch.profiler`, фиксировать метрики на кейсе `impes_large_multiwell_3d.json` в CI.  
5. **Регресс-тест без AMG**  
   • Добавить конфиг с `linear_solver.backend="torch_gmres"` и убедиться, что FI-решатель стабильно сходится без BoomerAMG.

> Эти пункты образуют «Sprint-1» перед переходом к много-GPU и поверхностной сети.

## 1. Видение

Создать открытый, масштабируемый и расширяемый симулятор, способный выступать физически достоверной средой для
обучения RL-агентов и LLM.

## 2. Крупные эпики и приоритеты

| Эпик | Цель | Приоритет |
|------|------|-----------|
| Инфраструктура и производительность | Рефакторинг, модульность, CI/CD, профилирование | 🔴 Высокий |
| Улучшение решателей | AMG/ILU предобусловленные решатели, GPU-Sparse | 🔴 Высокий |
| Расширенная модель скважин и трубопроводов | MSW, гидравлика столба, surface-network | 🔴 Высокий |
| 3-фазная физика (Black-Oil) | Водa-нефть-газ + PVT-таблицы | 🟠 Средний |
| Грид и геометрия | Corner-Point, LGR, переменный шаг | 🟠 Средний |
| Тепловые и химические процессы | Пар, полимер, ПАВ | 🟡 Низкий |
| Интеграция RL/LLM | Gym API, автоградиент, датасеты | 🟠 Средний |
| Историзация и калибровка | EnKF, автоматический матчинг | 🟡 Низкий |

## 3. Дорожная карта по кварталам (предложение)

### Базовый «v0.5»
1. **Рефакторинг кода**  
   • Разбить `simulation.py` на модули: `grid`, `physics`, `solver`, `io`.  
   • Добавить `pyproject.toml`, type-hints, `ruff`/`black` форматирование.  
2. **Профилирование и ускорение IMPES**  
   • Перейти c `for` циклов на векторизацию где возможно.  
   • Использовать `torch.jit` / `torch.compile`.
3. **Документация**  
   • Сгенерировать docs через Sphinx + MyST.  
   • Туториалы в Jupyter.

### «Расширенные скважины v0.6»
1. **Модель Peaceman с анизотропией и нестандартными ячейками**.  
2. **Многосегментные скважины (MSW)** — контроль по сегментам.  
3. **Трубопроводы и surface-network**  
   • Давление потерь (Beggs-Brill).  
   • Связка с резервуаром через скважинные сегменты.
4. **API управления**  
   • REST / gRPC слой для внешних оптимизаторов.

### «Black-Oil v0.7»
1. **Добавить газовую фазу** (трёхфазная модель).  
2. **PVT-таблицы** (ввод из Eclipse-формата).  
3. **Расширить решатель**: неявный трехфазный Jacobian.  
4. **Расширенные источники/приёмы: газлифт, ВУЭ**.

### «AI Integration v0.8»
1. **Gym-совместимый интерфейс** (`step`, `reset`, `observation_space`).  
2. **Dataset exporter** для offline-RL (HDF5).  
3. **Встроенный auto-diff** для градиентных методов управления.  
4. **LLM-помощник**: генерация конфигов и анализ логов.

### «HPC & Advanced Physics v1.0»
1. **Distributed Data Parallel** (torch.distributed) и MPI backend.  
2. **Алгебраическое многосеточное предобуславливание (Hypre/PyAMG)**.  
3. **Corner-Point Grid + LGR**.  
4. **Теплотехнические процессы (CSS/SAGD)**.  
5. **Химические EOR (Polymer, Surfactant)**.

## 4. Улучшения короткого цикла («быстрые победы»)
- Перевести параметры конфигов с JSON на YAML + Hydra.  
- Сохранение результатов в HDF5 вместо TXT/PNG для больших кейсов.  
- Добавить автоматическую проверку градиентов через `finite-diff` тесты.  
- Визуализация в реальном времени через `PyVista`/`panel`.

## 5. Интеграция с RL/LLM — детали
1. **Wrapper**: класс `ReservoirEnv` наследует `gym.Env`.  
2. **Observation**: карты давлений, насыщенности, состояние скважин.  
3. **Action**: управление дебитом / давлением, переключение режимов.  
4. **Reward**: NPV, добыча нефти, штраф за обводненность.  
5. **LLM**: цепочка рассуждений для генерации политик и объяснений решений.

## 6. Требуемые исследования R&D
- Сравнение имплицитного и импес по производительности на GPU.  
- Адаптивные локальные сеточные уточнения (AMR) в PyTorch.  
- Автоматическое построение оптимальных графов вычислений для скоростей.

## 7. Пожелания комьюнити
- Поддержка Windows и macOS.  
- Примеры кейсов: водо-напорное вытеснение, пятискважинный паттерн, SAGD.  
- Интеграция с `open-source` наборами керновых данных (SPE10).

## 8. Обратная связь и контрибьюции
PR и обсуждения приветствуются!  Используйте Issues для баг-репортов и предложений. 